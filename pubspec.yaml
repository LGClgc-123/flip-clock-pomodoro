name: flip_clock_pomodoro
description: "复古翻页时钟 + 番茄计时器"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # 降级至兼容 Dart3.3.0 的稳定版本
  shared_preferences: ^2.2.3
  flutter_local_notifications: ^18.0.1
  audioplayers: ^6.1.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
