name: flip_clock_pomodoro
description: "复古翻页时钟 + 番茄计时器"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.3
  provider: ^6.0.5

  # 暂时移除音频播放插件，规避安卓嵌入架构版本报错
  # audioplayers: ^5.2.1
  # 暂时移除本地通知插件，后续打包成功后再按需添加
  # flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
